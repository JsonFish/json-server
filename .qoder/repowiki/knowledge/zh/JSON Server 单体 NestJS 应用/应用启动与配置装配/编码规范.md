- 配置项以独立文件 + default export 的纯对象形式暴露，文件名以 `<服务>.config.ts` 命名，便于按依赖域拆分。
- 全局横切关注点（异常过滤、响应转换、鉴权）统一在 `AppModule` 中通过 `{ provide: APP_FILTER | APP_INTERCEPTOR | APP_GUARD, useClass: ... }` 令牌注册，而非在各控制器重复声明。