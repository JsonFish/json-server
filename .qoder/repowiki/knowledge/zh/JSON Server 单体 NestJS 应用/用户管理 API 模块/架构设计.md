遵循 NestJS 标准分层：`user.module.ts` 通过 `TypeOrmModule.forFeature([User])` 注入 `User` 实体仓库；`user.controller.ts` 暴露 `/user` 路由，仅做参数绑定与委托；`user.service.ts` 封装所有业务逻辑，直接依赖 `Repository<User>` 完成 CRUD；`entities/user.entity.ts` 使用 TypeORM 装饰器映射 `user` 表；`dto/user.dto.ts` 基于 `class-validator`/`class-transformer` 定义入参校验，并复用 `@/common/dto/pagination.dto` 实现分页。模块在 `exports: [UserService]` 中向外暴露服务供其他模块组合使用。当前文件存在重复的 `UserService` 类声明（第1-64行与第73-138行），后者为实际生效版本，前者应视为遗留副本。