- 统一响应体采用 `{ code, message, data }` 三字段结构，成功由 TransformInterceptor 注入，失败由各 ExceptionFilter 构造。
- 跨领域公共类型放在 `src/common/` 下并按职责子目录划分（如 dto），供 core 与其他业务模块共享。
- 可插拔的横切逻辑通过装饰器 + Reflector 元数据驱动（如 `@Public()` 配合 `AuthGuard`），而非硬编码白名单。