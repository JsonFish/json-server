- 控制器方法通过自定义 `@Public()` 装饰器标记无需鉴权的公开路由，其余路由由全局守卫拦截。
- 敏感配置集中从 `@/config/*.config` 导入（如 `jwt.config`、`github.config`），不在服务内硬编码。
- 对外错误统一抛出 `BadRequestException` 并附带中文提示消息，保持客户端可读的错误语义。
- DTO 使用 class-validator 装饰器（`@IsNotEmpty`、`@IsEmail`、`@MaxLength`）声明入参约束，而非手动校验。